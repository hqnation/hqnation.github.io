# The Site Configuration
theme: jekyll-theme-chirpy
lang: en
# Change to your timezone › https://zones.arilyn.cc
timezone: Europe/London

# jekyll-seo-tag settings › https://github.com/jekyll/jekyll-seo-tag/blob/master/docs/usage.md
title: Hq-Nation Blog
description: Hq-Nation - Github Blog Project.
url: "https://hqnation.github.io"
github:
  username: hqnation
twitter:
  username: prothesmos

social:
  name: Hq-Nation
  email: dr.chitkolinn@yahoo.com
  links:
    - https://twitter.com/prothesmos
    - https://github.com/prothesmos
    
# Site Verification Settings
webmaster_verifications:
  google: # fill in your Google verification code
  bing: # fill in your Bing verification code

# Web Analytics Settings
analytics:
  google:
    id: # fill in your Google Analytics ID
  cloudflare:
    id: # fill in your Cloudflare Web Analytics token
  
# Page views settings
pageviews:
  provider: # now only supports 'goatcounter'

# Prefer color scheme setting.
theme_mode: light

# The CDN endpoint for media resources.
cdn:

# the avatar on sidebar, support local or CORS resources
avatar: "https://upld.zone.id/uploads/eaid6qiq/hq.webp"
social_preview_image: "https://upld.zone.id/uploads/eaid6qiq/og.webp"

# boolean type, the global switch for TOC in posts.
toc: true

# comments:
  # Global switch for the post-comment system. Keeping it empty means disabled.
  # provider: # [disqus | utterances | giscus]
  # The provider options are as follows:
  # disqus:
  #   shortname: # fill with the Disqus shortname. › https://help.disqus.com/en/articles/1717111-what-s-a-shortname
  # utterances settings › https://utteranc.es/
  # utterances:
  #   repo: # <gh-username>/<repo>
  #   issue_term: # < url | pathname | title | ...>
  # Giscus options › https://giscus.app
  # giscus:
  #   repo: # <gh-username>/<repo>
  #   repo_id:
  #   category:
  #   category_id:
  #   mapping: # optional, default to 'pathname'
  #   strict: # optional, default to '0'
  #   input_position: # optional, default to 'bottom'
  #   lang: # optional, default to the value of `site.lang`
  #   reactions_enabled: # optional, default to the value of `1`

# Self-hosted static assets, optional › https://github.com/cotes2020/chirpy-static-assets
assets:
  self_host:
    enabled: true
    env: production

pwa:
  enabled: true
  cache:
    enabled: true
    deny_paths:
      # - "/example"  # URLs match `<SITE_URL>/exam*` will not be cached by the PWA

paginate: 10

# The base URL of your site
baseurl: ""

# ------------ The following options are not recommended to be modified ------------------
kramdown:
  footnote_backlink: "&#8617;&#xfe0e;"
  syntax_highlighter: rouge
  syntax_highlighter_opts: # Rouge Options › https://github.com/jneen/rouge#full-options
    css_class: highlight
    # default_lang: console
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
      comments: true
      toc: true
      permalink: /posts/:title/
  - scope:
      path: _drafts
    values:
      comments: false
  - scope:
      path: ""
      type: pages
    values:
      layout: page
      permalink: /pages/:title/

sass:
  style: compressed

compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - purgecss.js
  - "*.config.js"
  - "package*.json"

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/
